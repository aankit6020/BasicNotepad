<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheet/style.css
    ">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10 text-white">
            <form action="/create" method="post">
                <input class="block bg-zinc-700 px-5 py-5 m-5 rounded-md w-full text-xl font-semibold " type="text"
                    placeholder="Title" name="title">
                <textarea class="block bg-zinc-700 px-5 py-5 m-5 rounded-md w-full resize-none "
                    placeholder="write here.." name="details"></textarea>
                <input class="bg-blue-500 m-5 px-5 py-2 rounded-md " type="submit" value="Create Task">
            </form>
        </div>
        <div class="tasks flex gap-3 flex-wrap m-12">

            <% if(files.length > 0){ %>
             
                <% files.forEach(function(val){ %>
                    <div class="task w-72 px-3 py-4 rounded-md bg-zinc-700">
                        <h1 class="text-white text-2xl teacking-tighter mb-2 truncate"><%= val %> </h1>
                        <div class=" flex justify-between align-center">
                            <a class="text-blue-500 inline-block" href="/files/<%= val %>">Read More.</a>
                            <a href="/edit/<%= val %>" class="text-zinc-500"> edit</a>
                        </div>
                    </div>
                <% }) %>
                
            <% } else { %>
            <div class=" text-white m-5 text-2xl"> No more taks ...</div>
            <% } %>
            
        </div>

    </div>

    <script src="/javascript/script.js"></script>
</body>

</html>